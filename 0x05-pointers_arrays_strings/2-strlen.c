#include "holberton.h"

/**
 * _strlen - check the code for Holberton School students.
 * @s: s
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int cuenta = 0;

	for (cuenta = 0; s[cuenta] != '\0'; ++cuenta)

	return (cuenta);
}
